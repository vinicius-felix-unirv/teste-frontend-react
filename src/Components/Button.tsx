import {Button } from "@chakra-ui/react"
export const SistemButton = () => {
    return (
            <Button colorScheme='green' variant='outline' ml={115} mt={5}  width={250}>
                Entrar
            </Button>

    )
}